from setuptools import setup, find_packages

setup(name='DUN',
      version='0.0.1',
      description='Experiments for investigating Depth Uncertainty Networks.',
      packages=find_packages()
      )
